
cc.Class({
    extends: cc.Component,

    properties: {

    },

    // LIFE-CYCLE CALLBACKS:

    onLoad () {
        this.node.color = new cc.Color(255, 0, 0, 255);
    },

    // start () {

    // },

    // update (dt) {},
});
