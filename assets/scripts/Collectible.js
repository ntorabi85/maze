
cc.Class({
    extends: cc.Component,

    properties: {

    },

    // LIFE-CYCLE CALLBACKS:

    onLoad () {
        this.node.color = new cc.Color(0, 255, 0, 255);
    },

    // start () {

    // },

    // update (dt) {},
});
